import { BoughtGiftPipe } from "./bought-gift.pipe";

describe("BoughtGiftPipe", () => {
  it("create an instance", () => {
    const pipe = new BoughtGiftPipe();
    expect(pipe).toBeTruthy();
  });
});
